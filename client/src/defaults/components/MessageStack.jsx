var React = require('react/addons');

var MessageStack = React.createClass({
  mixins: [React.addons.PureRenderMixin],
  
  render: function() {
    return (
      <div/>
    );
  }
});

module.exports = MessageStack;